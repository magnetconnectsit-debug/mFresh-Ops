# Spec Kit Setup

This repository now has a lightweight Spec Kit-compatible workflow scaffold.

Use `AGENTS.md` for current agent context and `.specify/memory/constitution.md` for project principles.

Suggested flow:
1. Write or update the spec.
2. Turn the spec into an implementation plan.
3. Break the plan into tasks.
4. Implement and verify the change.
